<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ravinder.example.myklan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LocationTracking"></activity>
        <activity android:name=".TaskNotifications" />
        <activity android:name=".ContactDetails" />
        <activity android:name=".SharedContacts" />
        <activity android:name=".AccountDetails" />
        <activity android:name=".SharedAccounts" />
        <activity android:name=".SharedSpace" />
        <activity
            android:name=".Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Features" />
        <activity android:name=".userProfile" />
        <activity android:name=".FamilyMembers" />
        <activity android:name=".AddMemberForm" />
        <activity android:name=".whoAreYou" />
        <activity android:name=".MembersEmpty" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login"
            android:label="Login"
            android:theme="@style/AppTheme" />
    </application>

</manifest>